---
layout: base.njk
title: Endless Bounty
css: |
  /* Hero */
  .hero {
    text-align: center;
    padding: 4rem 2rem 3rem;
    background: linear-gradient(180deg, var(--soil) 0%, var(--soil-light) 100%);
  }

  .hero-emoji { font-size: 4rem; margin-bottom: 1rem; }

  .hero h1 {
    font-size: 3rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }

  .hero h1 span { color: var(--spinach); }

  .hero .tagline {
    font-size: 1.25rem;
    color: var(--straw);
    max-width: 500px;
    margin: 0 auto 2rem;
  }

  .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    font-weight: 600;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.3);
  }

  .btn-primary { background: var(--spinach); color: var(--soil); }
  .btn-secondary { background: var(--soil-light); color: var(--cream); border: 1px solid var(--sage); }

  /* Preview Section */
  .preview-section { padding: 3rem 2rem; }

  .preview-section h2 {
    text-align: center;
    font-size: 1.75rem;
    margin-bottom: 2rem;
    color: var(--straw);
  }

  .themes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .theme-card { border-radius: 12px; overflow: hidden; }
  .theme-card.dark { background: var(--soil); border: 1px solid var(--soil-light); }
  .theme-card.light { background: var(--canvas); border: 1px solid var(--canvas-dark); }

  .theme-header {
    padding: 1rem 1.25rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .dark .theme-header {
    background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
    color: var(--sage);
    border-bottom: 1px solid var(--soil-light);
  }

  .light .theme-header {
    background: {{ theme.light.colors['editorGroupHeader.tabsBackground'] }};
    color: #8a917d;
    border-bottom: 1px solid #ddd5c5;
  }

  .code-block {
    padding: 1.25rem;
    font-size: 12px;
    line-height: 1.6;
    overflow-x: auto;
  }

  .dark .code-block { background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }}; }
  .light .code-block { background: {{ theme.light.colors['editorGroupHeader.tabsBackground'] }}; color: #3d3629; }

  /* Syntax - Dark */
  .dark .comment { color: {{ theme.dark.syntax.comment }}; font-style: italic; }
  .dark .keyword { color: {{ theme.dark.syntax.keyword }}; }
  .dark .string { color: {{ theme.dark.syntax.string }}; }
  .dark .function { color: {{ theme.dark.syntax.function }}; }
  .dark .variable { color: {{ theme.dark.syntax.variable }}; }
  .dark .type { color: {{ theme.dark.syntax.type }}; }
  .dark .number { color: {{ theme.dark.syntax.number }}; }
  .dark .property { color: {{ theme.dark.syntax.property }}; }
  .dark .punctuation { color: {{ theme.dark.syntax.punctuation }}; }
  .dark .operator { color: {{ theme.dark.syntax.operator }}; }
  .dark .param { color: {{ theme.dark.syntax.parameter }}; font-style: italic; }
  .dark .cook-ing { color: {{ theme.dark.syntax.cookIngredient }}; font-weight: 600; }
  .dark .cook-ware { color: {{ theme.dark.syntax.cookCookware }}; font-weight: 600; }
  .dark .cook-time { color: {{ theme.dark.syntax.cookTimer }}; }
  .dark .cook-amt { color: {{ theme.dark.syntax.cookAmount }}; }
  .dark .cook-unit { color: {{ theme.dark.syntax.cookUnit }}; }
  .dark .cook-meta-k { color: {{ theme.dark.syntax.cookMetaKey }}; }
  .dark .cook-meta-v { color: {{ theme.dark.syntax.cookMetaValue }}; }

  /* Syntax - Light */
  .light .comment { color: {{ theme.light.syntax.comment }}; font-style: italic; }
  .light .keyword { color: {{ theme.light.syntax.keyword }}; }
  .light .string { color: {{ theme.light.syntax.string }}; }
  .light .function { color: {{ theme.light.syntax.function }}; }
  .light .variable { color: {{ theme.light.syntax.variable }}; }
  .light .type { color: {{ theme.light.syntax.type }}; }
  .light .number { color: {{ theme.light.syntax.number }}; }
  .light .property { color: {{ theme.light.syntax.property }}; }
  .light .punctuation { color: {{ theme.light.syntax.punctuation }}; }
  .light .operator { color: {{ theme.light.syntax.operator }}; }
  .light .param { color: {{ theme.light.syntax.parameter }}; font-style: italic; }
  .light .cook-ing { color: {{ theme.light.syntax.cookIngredient }}; font-weight: 600; }
  .light .cook-ware { color: {{ theme.light.syntax.cookCookware }}; font-weight: 600; }
  .light .cook-time { color: {{ theme.light.syntax.cookTimer }}; }
  .light .cook-amt { color: {{ theme.light.syntax.cookAmount }}; }
  .light .cook-unit { color: {{ theme.light.syntax.cookUnit }}; }
  .light .cook-meta-k { color: {{ theme.light.syntax.cookMetaKey }}; }
  .light .cook-meta-v { color: {{ theme.light.syntax.cookMetaValue }}; }

  /* Status Bar Preview */
  .status-bar-section { padding: 3rem 2rem; }

  .status-bar-section h2 {
    text-align: center;
    font-size: 1.75rem;
    margin-bottom: 0.5rem;
    color: var(--straw);
  }

  .status-bar-section .subtitle {
    text-align: center;
    color: var(--sage);
    margin-bottom: 2rem;
  }

  .status-bars {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .status-bar-preview {
    border-radius: 8px;
    overflow: hidden;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
  }

  .status-bar-preview .bar {
    display: flex;
    align-items: center;
    padding: 0 0.5rem;
    height: 22px;
    gap: 0.5rem;
  }

  .status-bar-preview .bar-item {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0 6px;
    height: 100%;
  }

  .status-bar-preview .spacer { flex: 1; }

  .status-bar-preview .remote-btn {
    padding: 0 8px;
    margin-left: -0.5rem;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .status-bar-preview.dark .bar {
    background: {{ theme.dark.colors['statusBar.background'] }};
    color: {{ theme.dark.colors['statusBar.foreground'] }};
  }

  .status-bar-preview.dark .remote-btn {
    background: {{ theme.dark.colors['statusBarItem.remoteBackground'] }};
    color: {{ theme.dark.colors['statusBarItem.remoteForeground'] }};
  }

  .status-bar-preview.light .bar {
    background: {{ theme.light.colors['statusBar.background'] }};
    color: {{ theme.light.colors['statusBar.foreground'] }};
  }

  .status-bar-preview.light .remote-btn {
    background: {{ theme.light.colors['statusBarItem.remoteBackground'] }};
    color: {{ theme.light.colors['statusBarItem.remoteForeground'] }};
  }

  .status-bar-label {
    text-align: center;
    margin-top: 0.5rem;
    font-size: 0.75rem;
    color: var(--sage);
    font-family: 'JetBrains Mono', monospace;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  /* Palette Section */
  .palette-section {
    padding: 3rem 2rem;
    background: var(--soil-light);
  }

  .palette-section h2 {
    text-align: center;
    font-size: 1.75rem;
    margin-bottom: 0.5rem;
  }

  .palette-section .subtitle {
    text-align: center;
    color: var(--sage);
    margin-bottom: 2rem;
  }

  .palette-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 1rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .swatch {
    aspect-ratio: 1;
    border-radius: 12px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-family: 'JetBrains Mono', monospace;
    transition: transform 0.15s ease;
  }

  .swatch:hover { transform: scale(1.05); }
  .swatch-name { font-weight: 600; font-size: 0.85rem; margin-bottom: 0.25rem; }
  .swatch-hex { font-size: 0.7rem; opacity: 0.8; }

  .sw-spinach { background: var(--spinach); color: var(--soil); }
  .sw-carrot { background: var(--carrot); color: var(--soil); }
  .sw-apple { background: var(--apple); color: #fff; }
  .sw-squash { background: var(--squash); color: var(--soil); }
  .sw-eggplant { background: var(--eggplant); color: #fff; }
  .sw-honey { background: var(--honey); color: var(--soil); }
  .sw-tomato { background: var(--tomato); color: #fff; }
  .sw-sage { background: var(--sage); color: #fff; }

  /* Cooklang Section */
  .cooklang-section {
    padding: 3rem 2rem;
    text-align: center;
  }

  .cooklang-section h2 { font-size: 1.75rem; margin-bottom: 0.5rem; }
  .cooklang-section p { color: var(--straw); max-width: 550px; margin: 0 auto 1.5rem; }
  .cooklang-section a { color: var(--spinach); }

  .cooklang-features {
    display: flex;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
    margin-top: 1.5rem;
  }

  .cooklang-feature { font-family: 'JetBrains Mono', monospace; font-size: 0.9rem; }
  .cooklang-feature span { display: block; font-size: 0.75rem; color: var(--sage); margin-top: 0.25rem; }

  .file-types {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 1.5rem;
    flex-wrap: wrap;
  }

  .file-types code {
    background: var(--soil-light);
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.85rem;
    color: var(--straw);
  }

  /* Examples Section */
  .examples-section {
    padding: 3rem 2rem;
    background: var(--soil-light);
  }

  .examples-section h2 {
    text-align: center;
    font-size: 1.75rem;
    margin-bottom: 2rem;
    color: var(--straw);
  }

  .examples-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .example-card { border-radius: 12px; overflow: hidden; }
  .example-card.dark { background: var(--soil); border: 1px solid #2a2420; }

  .example-header {
    padding: 0.75rem 1rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
    color: var(--sage);
    border-bottom: 1px solid #2a2420;
  }

  .example-card .code-block {
    font-size: 11px;
    line-height: 1.5;
    padding: 1rem;
    background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
  }

  /* Footer */
  footer {
    padding: 2rem;
    text-align: center;
    border-top: 1px solid var(--soil-light);
    color: var(--sage);
    font-size: 0.9rem;
  }

  footer a { color: var(--straw); }

  @media (max-width: 600px) {
    .hero h1 { font-size: 2rem; }
    .hero .tagline { font-size: 1rem; }
    .buttons { flex-direction: column; align-items: center; }
    .themes-grid { grid-template-columns: 1fr; }
  }
---

<!-- Hero -->
<section class="hero">
  <div class="hero-emoji">🥕</div>
  <h1>Endless <span>Bounty</span></h1>
  <p class="tagline">A warm, organic VS Code theme inspired by fresh-picked vegetables at the farmers market.</p>
  <div class="buttons">
    <a href="https://marketplace.visualstudio.com/items?itemName=chronick.endless-bounty" class="btn btn-primary">
      Install Extension
    </a>
    <a href="https://github.com/chronick/endless-bounty" class="btn btn-secondary">
      View on GitHub
    </a>
  </div>
</section>

<!-- Preview -->
<section class="preview-section">
  <h2>Two Variants</h2>
  <div class="themes-grid">

    <!-- Dark Theme -->
    <div class="theme-card dark">
      <div class="theme-header">Dark — Evening Market</div>
      <div class="code-block">
<span class="comment">// TypeScript</span>
<span class="keyword">interface</span> <span class="type">Vegetable</span> <span class="punctuation">{</span>
  <span class="property">name</span><span class="punctuation">:</span> <span class="type">string</span><span class="punctuation">;</span>
  <span class="property">organic</span><span class="punctuation">:</span> <span class="type">boolean</span><span class="punctuation">;</span>
<span class="punctuation">}</span>

<span class="keyword">const</span> <span class="variable">basket</span> <span class="operator">=</span> <span class="punctuation">(</span><span class="param">items</span><span class="punctuation">)</span> <span class="keyword">=></span>
  <span class="variable">items</span><span class="punctuation">.</span><span class="function">filter</span><span class="punctuation">(</span><span class="param">v</span> <span class="keyword">=></span> <span class="param">v</span><span class="punctuation">.</span><span class="property">organic</span><span class="punctuation">);</span>

<span class="comment">-- Cooklang Recipe</span>
<span class="cook-meta-k">>> servings:</span> <span class="cook-meta-v">4</span>

Sauté <span class="cook-ing">@onion</span><span class="punctuation">{</span><span class="cook-amt">1</span><span class="punctuation">%</span><span class="cook-unit">medium</span><span class="punctuation">}</span> in a
<span class="cook-ware">#large skillet</span> for <span class="cook-time">~{5%min}</span>.</div>
    </div>

    <!-- Light Theme -->
    <div class="theme-card light">
      <div class="theme-header">Light — Morning Market</div>
      <div class="code-block">
<span class="comment">// TypeScript</span>
<span class="keyword">interface</span> <span class="type">Vegetable</span> <span class="punctuation">{</span>
  <span class="property">name</span><span class="punctuation">:</span> <span class="type">string</span><span class="punctuation">;</span>
  <span class="property">organic</span><span class="punctuation">:</span> <span class="type">boolean</span><span class="punctuation">;</span>
<span class="punctuation">}</span>

<span class="keyword">const</span> <span class="variable">basket</span> <span class="operator">=</span> <span class="punctuation">(</span><span class="param">items</span><span class="punctuation">)</span> <span class="keyword">=></span>
  <span class="variable">items</span><span class="punctuation">.</span><span class="function">filter</span><span class="punctuation">(</span><span class="param">v</span> <span class="keyword">=></span> <span class="param">v</span><span class="punctuation">.</span><span class="property">organic</span><span class="punctuation">);</span>

<span class="comment">-- Cooklang Recipe</span>
<span class="cook-meta-k">>> servings:</span> <span class="cook-meta-v">4</span>

Sauté <span class="cook-ing">@onion</span><span class="punctuation">{</span><span class="cook-amt">1</span><span class="punctuation">%</span><span class="cook-unit">medium</span><span class="punctuation">}</span> in a
<span class="cook-ware">#large skillet</span> for <span class="cook-time">~{5%min}</span>.</div>
    </div>

  </div>
</section>

<!-- Status Bar Preview -->
<section class="status-bar-section">
  <h2>Thoughtful UI</h2>
  <p class="subtitle">Status bars that complement your workspace</p>
  <div class="status-bars">
    <div>
      <div class="status-bar-preview dark">
        <div class="bar">
          <span class="remote-btn">◊ WSL</span>
          <span class="bar-item">main*</span>
          <span class="bar-item">⟳ 0 ↓ 0</span>
          <span class="spacer"></span>
          <span class="bar-item">Ln 42, Col 18</span>
          <span class="bar-item">UTF-8</span>
          <span class="bar-item">TypeScript</span>
        </div>
      </div>
      <div class="status-bar-label">Dark Theme</div>
    </div>
    <div>
      <div class="status-bar-preview light">
        <div class="bar">
          <span class="remote-btn">◊ SSH</span>
          <span class="bar-item">develop</span>
          <span class="bar-item">⟳ 2 ↓ 1</span>
          <span class="spacer"></span>
          <span class="bar-item">Ln 156, Col 4</span>
          <span class="bar-item">UTF-8</span>
          <span class="bar-item">Python</span>
        </div>
      </div>
      <div class="status-bar-label">Light Theme</div>
    </div>
  </div>
</section>

<!-- Palette -->
<section class="palette-section">
  <h2>The Harvest</h2>
  <p class="subtitle">Every color tells a story</p>
  <div class="palette-grid">
    <div class="swatch sw-spinach"><span class="swatch-name">Spinach</span><span class="swatch-hex">strings</span></div>
    <div class="swatch sw-carrot"><span class="swatch-name">Carrot</span><span class="swatch-hex">keywords</span></div>
    <div class="swatch sw-apple"><span class="swatch-name">Apple</span><span class="swatch-hex">functions</span></div>
    <div class="swatch sw-squash"><span class="swatch-name">Squash</span><span class="swatch-hex">variables</span></div>
    <div class="swatch sw-eggplant"><span class="swatch-name">Eggplant</span><span class="swatch-hex">types</span></div>
    <div class="swatch sw-honey"><span class="swatch-name">Honey</span><span class="swatch-hex">numbers</span></div>
    <div class="swatch sw-tomato"><span class="swatch-name">Tomato</span><span class="swatch-hex">constants</span></div>
    <div class="swatch sw-sage"><span class="swatch-name">Sage</span><span class="swatch-hex">comments</span></div>
  </div>
</section>

<!-- Cooklang -->
<section class="cooklang-section">
  <h2>First-Class Cooklang Support</h2>
  <p>
    We love <a href="https://cooklang.org/" target="_blank" rel="noopener">Cooklang</a> — the recipe markup language that lets you manage recipes as plain text files. Endless Bounty includes syntax highlighting for all Cooklang formats.
  </p>
  <div class="cooklang-features">
    <div class="cooklang-feature" style="color: var(--spinach);">@ingredients<span>spinach green</span></div>
    <div class="cooklang-feature" style="color: var(--apple);">#cookware<span>apple red</span></div>
    <div class="cooklang-feature" style="color: var(--eggplant);">~{timers}<span>eggplant purple</span></div>
    <div class="cooklang-feature" style="color: var(--honey);">{amounts}<span>honey gold</span></div>
  </div>
  <div class="file-types">
    <code>.cook</code> recipes
    <code>.menu</code> meal plans
    <code>aisle.conf</code> shopping config
  </div>
</section>

<!-- Cooklang Examples -->
<section class="examples-section">
  <h2>Cooklang Examples</h2>
  <div class="examples-grid">

    <!-- .cook example - Grandma's Pot Roast -->
    <div class="example-card dark">
      <div class="example-header">.cook — Sunday Pot Roast</div>
      <div class="code-block">
<span class="cook-meta-k">>> source:</span> <span class="cook-meta-v">grandma's recipe box</span>
<span class="cook-meta-k">>> servings:</span> <span class="cook-meta-v">6</span>
<span class="cook-meta-k">>> time:</span> <span class="cook-meta-v">4 hours</span>

<span class="comment">-- A Sunday family tradition</span>

Season <span class="cook-ing">@chuck roast</span><span class="punctuation">{</span><span class="cook-amt">3</span><span class="punctuation">%</span><span class="cook-unit">lb</span><span class="punctuation">}</span> generously
with <span class="cook-ing">@salt</span> and <span class="cook-ing">@black pepper</span>.

Sear all sides in a <span class="cook-ware">#Dutch oven</span>
over high heat for <span class="cook-time">~{3%min}</span> per side.

Add <span class="cook-ing">@carrots</span><span class="punctuation">{</span><span class="cook-amt">4</span><span class="punctuation">%</span><span class="cook-unit">large</span><span class="punctuation">}</span>,
<span class="cook-ing">@potatoes</span><span class="punctuation">{</span><span class="cook-amt">6</span><span class="punctuation">%</span><span class="cook-unit">medium</span><span class="punctuation">}</span>,
<span class="cook-ing">@onion</span><span class="punctuation">{</span><span class="cook-amt">2</span><span class="punctuation">%</span><span class="cook-unit">large</span><span class="punctuation">}</span> quartered,
and <span class="cook-ing">@beef broth</span><span class="punctuation">{</span><span class="cook-amt">2</span><span class="punctuation">%</span><span class="cook-unit">cups</span><span class="punctuation">}</span>.

Cover and braise at 325°F for <span class="cook-time">~{3%hours}</span>.</div>
    </div>

    <!-- .menu example -->
    <div class="example-card dark">
      <div class="example-header">.menu — Family Week</div>
      <div class="code-block">
<span class="punctuation">---</span>
<span class="cook-meta-k">servings:</span> <span class="cook-meta-v">4</span>
<span class="cook-meta-k">week:</span> <span class="cook-meta-v">January 13-19</span>
<span class="punctuation">---</span>

<span class="keyword">== Sunday ==</span>
<span class="comment">-- meal prep day</span>
<span class="punctuation">-</span> <span class="cook-ing">@./Mains/Pot Roast</span>
<span class="punctuation">-</span> <span class="cook-ing">@./Sides/Roasted Vegetables</span>

<span class="keyword">== Monday ==</span>
<span class="punctuation">-</span> Leftover pot roast sandwiches
<span class="punctuation">-</span> <span class="cook-ing">@./Salads/Caesar</span><span class="punctuation">{</span><span class="cook-amt">4</span><span class="punctuation">%</span><span class="cook-unit">servings</span><span class="punctuation">}</span>

<span class="keyword">== Tuesday ==</span>
<span class="punctuation">-</span> <span class="cook-ing">@./Quick/Pasta Primavera</span>
<span class="punctuation">-</span> <span class="cook-ing">@garlic bread</span><span class="punctuation">{</span><span class="cook-amt">1</span><span class="punctuation">%</span><span class="cook-unit">loaf</span><span class="punctuation">}</span>

<span class="keyword">== Snacks ==</span>
<span class="punctuation">-</span> <span class="cook-ing">@apples</span><span class="punctuation">{</span><span class="cook-amt">8</span><span class="punctuation">}</span>
<span class="punctuation">-</span> <span class="cook-ing">@cheese sticks</span><span class="punctuation">{</span><span class="cook-amt">12</span><span class="punctuation">}</span>
<span class="punctuation">-</span> <span class="cook-ing">@trail mix</span><span class="punctuation">{</span><span class="cook-amt">2</span><span class="punctuation">%</span><span class="cook-unit">cups</span><span class="punctuation">}</span></div>
    </div>

    <!-- aisle.conf example -->
    <div class="example-card dark">
      <div class="example-header">aisle.conf — Grocery Store Layout</div>
      <div class="code-block">
<span class="comment"># Our local grocery store layout</span>
<span class="comment"># Organized by aisle for efficient shopping</span>

<span class="keyword">[produce]</span>
<span class="variable">carrots</span>
<span class="variable">potatoes</span>
<span class="variable">onion</span><span class="punctuation">|</span><span class="string">onions</span><span class="punctuation">|</span><span class="string">yellow onion</span>
<span class="variable">garlic</span>
<span class="variable">apples</span>
<span class="variable">lettuce</span><span class="punctuation">|</span><span class="string">romaine</span>

<span class="keyword">[meat]</span>
<span class="variable">chuck roast</span><span class="punctuation">|</span><span class="string">beef roast</span>
<span class="variable">ground beef</span>
<span class="variable">chicken breast</span>

<span class="keyword">[dairy]</span>
<span class="variable">butter</span><span class="punctuation">|</span><span class="string">unsalted butter</span>
<span class="variable">cheese sticks</span>
<span class="variable">parmesan</span><span class="punctuation">|</span><span class="string">parmigiano</span>

<span class="keyword">[pantry]</span>
<span class="variable">beef broth</span><span class="punctuation">|</span><span class="string">stock</span>
<span class="variable">olive oil</span>
<span class="variable">pasta</span><span class="punctuation">|</span><span class="string">penne</span><span class="punctuation">|</span><span class="string">spaghetti</span></div>
    </div>

  </div>
</section>

<!-- Footer -->
<footer>
  <p>
    Made with 🥬 · <a href="https://github.com/chronick/endless-bounty">GitHub</a> · <a href="https://cooklang.org/">Cooklang</a>
  </p>
  <p style="margin-top: 0.5rem; font-style: italic; opacity: 0.7;">
    Locally sourced colors. No artificial flavors.
  </p>
</footer>
