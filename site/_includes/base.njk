<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} — VS Code Theme</title>
  <meta name="description" content="A warm, organic VS Code color theme inspired by fresh-picked vegetables at the farmers market.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600&family=Source+Serif+4:opsz,wght@8..60,400;8..60,600&display=swap" rel="stylesheet">
  <style>
    :root {
      --soil: {{ theme.dark.colors['editor.background'] }};
      --soil-light: {{ theme.dark.colors['sideBar.background'] }};
      --canvas: {{ theme.light.colors['editor.background'] }};
      --canvas-dark: {{ theme.light.colors['sideBar.background'] }};
      --spinach: {{ theme.palette.spinach }};
      --carrot: {{ theme.palette.carrot }};
      --apple: {{ theme.palette.apple }};
      --squash: {{ theme.palette.squash }};
      --eggplant: {{ theme.palette.eggplant }};
      --honey: {{ theme.palette.honey }};
      --tomato: {{ theme.palette.tomato }};
      --sage: {{ theme.palette.sage }};
      --straw: {{ theme.dark.colors['editor.foreground'] }};
      --cream: {{ theme.dark.colors['sideBar.foreground'] }};
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Source Serif 4', Georgia, serif;
      background: var(--soil);
      color: var(--cream);
      line-height: 1.6;
    }

    code, .code-block, .mono {
      font-family: 'JetBrains Mono', monospace;
    }

    /* Hero */
    .hero {
      text-align: center;
      padding: 4rem 2rem 3rem;
      background: linear-gradient(180deg, var(--soil) 0%, var(--soil-light) 100%);
    }

    .hero-emoji { font-size: 4rem; margin-bottom: 1rem; }

    .hero h1 {
      font-size: 3rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      letter-spacing: -0.02em;
    }

    .hero h1 span { color: var(--spinach); }

    .hero .tagline {
      font-size: 1.25rem;
      color: var(--straw);
      max-width: 500px;
      margin: 0 auto 2rem;
    }

    .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.875rem 1.5rem;
      border-radius: 8px;
      text-decoration: none;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.9rem;
      font-weight: 600;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(0,0,0,0.3);
    }

    .btn-primary { background: var(--spinach); color: var(--soil); }
    .btn-secondary { background: var(--soil-light); color: var(--cream); border: 1px solid var(--sage); }

    /* Preview Section */
    .preview-section { padding: 3rem 2rem; }

    .preview-section h2 {
      text-align: center;
      font-size: 1.75rem;
      margin-bottom: 2rem;
      color: var(--straw);
    }

    .themes-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
      gap: 2rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    .theme-card { border-radius: 12px; overflow: hidden; }
    .theme-card.dark { background: var(--soil); border: 1px solid var(--soil-light); }
    .theme-card.light { background: var(--canvas); border: 1px solid var(--canvas-dark); }

    .theme-header {
      padding: 1rem 1.25rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .dark .theme-header {
      background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
      color: var(--sage);
      border-bottom: 1px solid var(--soil-light);
    }

    .light .theme-header {
      background: {{ theme.light.colors['editorGroupHeader.tabsBackground'] }};
      color: #8a917d;
      border-bottom: 1px solid #ddd5c5;
    }

    .code-block {
      padding: 1.25rem;
      font-size: 12px;
      line-height: 1.6;
      overflow-x: auto;
      white-space: pre-wrap;
    }

    .dark .code-block { background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }}; }
    .light .code-block { background: {{ theme.light.colors['editorGroupHeader.tabsBackground'] }}; color: #3d3629; }

    /* Syntax - Dark */
    .dark .comment { color: {{ theme.dark.syntax.comment }}; font-style: italic; }
    .dark .keyword { color: {{ theme.dark.syntax.keyword }}; }
    .dark .string { color: {{ theme.dark.syntax.string }}; }
    .dark .function { color: {{ theme.dark.syntax.function }}; }
    .dark .variable { color: {{ theme.dark.syntax.variable }}; }
    .dark .type { color: {{ theme.dark.syntax.type }}; }
    .dark .number { color: {{ theme.dark.syntax.number }}; }
    .dark .property { color: {{ theme.dark.syntax.property }}; }
    .dark .punctuation { color: {{ theme.dark.syntax.punctuation }}; }
    .dark .operator { color: {{ theme.dark.syntax.operator }}; }
    .dark .param { color: {{ theme.dark.syntax.parameter }}; font-style: italic; }
    .dark .cook-ing { color: {{ theme.dark.syntax.cookIngredient }}; font-weight: 600; }
    .dark .cook-ware { color: {{ theme.dark.syntax.cookCookware }}; font-weight: 600; }
    .dark .cook-time { color: {{ theme.dark.syntax.cookTimer }}; }
    .dark .cook-amt { color: {{ theme.dark.syntax.cookAmount }}; }
    .dark .cook-unit { color: {{ theme.dark.syntax.cookUnit }}; }
    .dark .cook-meta-k { color: {{ theme.dark.syntax.cookMetaKey }}; }
    .dark .cook-meta-v { color: {{ theme.dark.syntax.cookMetaValue }}; }

    /* Syntax - Light */
    .light .comment { color: {{ theme.light.syntax.comment }}; font-style: italic; }
    .light .keyword { color: {{ theme.light.syntax.keyword }}; }
    .light .string { color: {{ theme.light.syntax.string }}; }
    .light .function { color: {{ theme.light.syntax.function }}; }
    .light .variable { color: {{ theme.light.syntax.variable }}; }
    .light .type { color: {{ theme.light.syntax.type }}; }
    .light .number { color: {{ theme.light.syntax.number }}; }
    .light .property { color: {{ theme.light.syntax.property }}; }
    .light .punctuation { color: {{ theme.light.syntax.punctuation }}; }
    .light .operator { color: {{ theme.light.syntax.operator }}; }
    .light .param { color: {{ theme.light.syntax.parameter }}; font-style: italic; }
    .light .cook-ing { color: {{ theme.light.syntax.cookIngredient }}; font-weight: 600; }
    .light .cook-ware { color: {{ theme.light.syntax.cookCookware }}; font-weight: 600; }
    .light .cook-time { color: {{ theme.light.syntax.cookTimer }}; }
    .light .cook-amt { color: {{ theme.light.syntax.cookAmount }}; }
    .light .cook-unit { color: {{ theme.light.syntax.cookUnit }}; }
    .light .cook-meta-k { color: {{ theme.light.syntax.cookMetaKey }}; }
    .light .cook-meta-v { color: {{ theme.light.syntax.cookMetaValue }}; }

    /* Status Bar Preview */
    .status-bar-section { padding: 3rem 2rem; }

    .status-bar-section h2 {
      text-align: center;
      font-size: 1.75rem;
      margin-bottom: 0.5rem;
      color: var(--straw);
    }

    .status-bar-section .subtitle {
      text-align: center;
      color: var(--sage);
      margin-bottom: 2rem;
    }

    .status-bars {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
      gap: 2rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    .status-bar-preview {
      border-radius: 8px;
      overflow: hidden;
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
    }

    .status-bar-preview .bar {
      display: flex;
      align-items: center;
      padding: 0 0.5rem;
      height: 22px;
      gap: 0.5rem;
    }

    .status-bar-preview .bar-item {
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 0 6px;
      height: 100%;
    }

    .status-bar-preview .spacer { flex: 1; }

    .status-bar-preview .remote-btn {
      padding: 0 8px;
      margin-left: -0.5rem;
      height: 100%;
      display: flex;
      align-items: center;
    }

    .status-bar-preview.dark .bar {
      background: {{ theme.dark.colors['statusBar.background'] }};
      color: {{ theme.dark.colors['statusBar.foreground'] }};
    }

    .status-bar-preview.dark .remote-btn {
      background: {{ theme.dark.colors['statusBarItem.remoteBackground'] }};
      color: {{ theme.dark.colors['statusBarItem.remoteForeground'] }};
    }

    .status-bar-preview.light .bar {
      background: {{ theme.light.colors['statusBar.background'] }};
      color: {{ theme.light.colors['statusBar.foreground'] }};
    }

    .status-bar-preview.light .remote-btn {
      background: {{ theme.light.colors['statusBarItem.remoteBackground'] }};
      color: {{ theme.light.colors['statusBarItem.remoteForeground'] }};
    }

    .status-bar-label {
      text-align: center;
      margin-top: 0.5rem;
      font-size: 0.75rem;
      color: var(--sage);
      font-family: 'JetBrains Mono', monospace;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    /* Palette Section */
    .palette-section {
      padding: 3rem 2rem;
      background: var(--soil-light);
    }

    .palette-section h2 {
      text-align: center;
      font-size: 1.75rem;
      margin-bottom: 0.5rem;
    }

    .palette-section .subtitle {
      text-align: center;
      color: var(--sage);
      margin-bottom: 2rem;
    }

    .palette-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 1rem;
      max-width: 800px;
      margin: 0 auto;
    }

    .swatch {
      aspect-ratio: 1;
      border-radius: 12px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: 'JetBrains Mono', monospace;
      transition: transform 0.15s ease;
    }

    .swatch:hover { transform: scale(1.05); }
    .swatch-name { font-weight: 600; font-size: 0.85rem; margin-bottom: 0.25rem; }
    .swatch-hex { font-size: 0.7rem; opacity: 0.8; }

    .sw-spinach { background: var(--spinach); color: var(--soil); }
    .sw-carrot { background: var(--carrot); color: var(--soil); }
    .sw-apple { background: var(--apple); color: #fff; }
    .sw-squash { background: var(--squash); color: var(--soil); }
    .sw-eggplant { background: var(--eggplant); color: #fff; }
    .sw-honey { background: var(--honey); color: var(--soil); }
    .sw-tomato { background: var(--tomato); color: #fff; }
    .sw-sage { background: var(--sage); color: #fff; }

    /* Cooklang Section */
    .cooklang-section {
      padding: 3rem 2rem;
      text-align: center;
    }

    .cooklang-section h2 { font-size: 1.75rem; margin-bottom: 0.5rem; }
    .cooklang-section p { color: var(--straw); max-width: 550px; margin: 0 auto 1.5rem; }
    .cooklang-section a { color: var(--spinach); }

    .cooklang-features {
      display: flex;
      justify-content: center;
      gap: 2rem;
      flex-wrap: wrap;
      margin-top: 1.5rem;
    }

    .cooklang-feature { font-family: 'JetBrains Mono', monospace; font-size: 0.9rem; }
    .cooklang-feature span { display: block; font-size: 0.75rem; color: var(--sage); margin-top: 0.25rem; }

    .file-types {
      display: flex;
      justify-content: center;
      gap: 1.5rem;
      margin-top: 1.5rem;
      flex-wrap: wrap;
    }

    .file-types code {
      background: var(--soil-light);
      padding: 0.5rem 1rem;
      border-radius: 6px;
      font-size: 0.85rem;
      color: var(--straw);
    }

    /* Examples Section */
    .examples-section {
      padding: 3rem 2rem;
      background: var(--soil-light);
    }

    .examples-section h2 {
      text-align: center;
      font-size: 1.75rem;
      margin-bottom: 2rem;
      color: var(--straw);
    }

    .examples-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .example-card { border-radius: 12px; overflow: hidden; }
    .example-card.dark { background: var(--soil); border: 1px solid #2a2420; }

    .example-header {
      padding: 0.75rem 1rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.75rem;
      background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
      color: var(--sage);
      border-bottom: 1px solid #2a2420;
    }

    .example-card .code-block {
      font-size: 11px;
      line-height: 1.5;
      padding: 1rem;
      background: {{ theme.dark.colors['editorGroupHeader.tabsBackground'] }};
    }

    /* Footer */
    footer {
      padding: 2rem;
      text-align: center;
      border-top: 1px solid var(--soil-light);
      color: var(--sage);
      font-size: 0.9rem;
    }

    footer a { color: var(--straw); }

    @media (max-width: 600px) {
      .hero h1 { font-size: 2rem; }
      .hero .tagline { font-size: 1rem; }
      .buttons { flex-direction: column; align-items: center; }
      .themes-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  {{ content | safe }}
</body>
</html>
